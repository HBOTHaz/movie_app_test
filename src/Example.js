// we don't using this component. just example!

import React from 'react';
// 새 컴포넌트 작성 할 때마다 이를 써줘야 함

function Example() {
    return (
        <b>I made it, just new Component! well. let's see...</b>
    )
}

export default Example;